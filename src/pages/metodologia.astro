---
/*
  Archivo: metodologia.astro
  Propósito: Página de Metodología IA
  Inspirado en: asisteclick.com/asistedeploy/
*/
import MainLayout from '../layouts/MainLayout.astro';
import { Icon } from 'astro-icon/components';

// Datos de las fases de la metodología
const fasesMetodologia = [
  {
    id: 'analisis',
    titulo: 'Análisis y Diagnóstico',
    descripcion: 'Evaluamos tus necesidades y desafíos específicos para diseñar una solución de IA a medida.',
    icono: '🔍',
    pasos: [
      'Reunión de descubrimiento',
      'Análisis de datos existentes',
      'Definición de KPIs',
      'Evaluación de infraestructura'
    ],
    duracion: '1-2 semanas',
    color: 'from-blue-400 to-cyan-400'
  },
  {
    id: 'diseno',
    titulo: 'Diseño de Solución',
    descripcion: 'Creamos una arquitectura personalizada que se integra perfectamente con tus sistemas actuales.',
    icono: '🎨',
    pasos: [
      'Diseño de arquitectura',
      'Selección de tecnologías',
      'Plan de integración',
      'Diseño de interfaces'
    ],
    duracion: '2-3 semanas',
    color: 'from-purple-400 to-pink-400'
  },
  {
    id: 'desarrollo',
    titulo: 'Desarrollo y Entrenamiento',
    descripcion: 'Construimos y entrenamos los modelos de IA con tus datos para garantizar los mejores resultados.',
    icono: '⚙️',
    pasos: [
      'Preparación de datos',
      'Entrenamiento de modelos',
      'Validación de resultados',
      'Optimización de rendimiento'
    ],
    duracion: '3-6 semanas',
    color: 'from-yellow-400 to-orange-400'
  },
  {
    id: 'implementacion',
    titulo: 'Implementación',
    descripcion: 'Desplegamos la solución en tu entorno con el mínimo impacto en tus operaciones actuales.',
    icono: '🚀',
    pasos: [
      'Pruebas de integración',
      'Migración de datos',
      'Puesta en producción',
      'Monitoreo inicial'
    ],
    duracion: '1-2 semanas',
    color: 'from-green-400 to-emerald-400'
  },
  {
    id: 'optimizacion',
    titulo: 'Optimización Continua',
    descripcion: 'Monitoreamos y mejoramos continuamente el rendimiento de la solución.',
    icono: '📊',
    pasos: [
      'Análisis de métricas',
      'Ajuste de parámetros',
      'Actualizaciones periódicas',
      'Soporte continuo'
    ],
    duracion: 'Continuo',
    color: 'from-red-400 to-pink-400'
  }
];

// Beneficios clave
const beneficios = [
  {
    titulo: 'Enfoque Personalizado',
    descripcion: 'Soluciones adaptadas a tus necesidades específicas.',
    icono: '🎯'
  },
  {
    titulo: 'Tecnología de Vanguardia',
    descripcion: 'Utilizamos las últimas tecnologías en IA y machine learning.',
    icono: '🚀'
  },
  {
    titulo: 'Resultados Medibles',
    descripcion: 'Enfoque basado en datos y métricas claras de éxito.',
    icono: '📈'
  },
  {
    titulo: 'Soporte Continuo',
    descripcion: 'Acompañamiento en cada paso del proceso.',
    icono: '🛡️'
  }
];

// Preguntas frecuentes
const preguntasFrecuentes = [
  {
    pregunta: '¿Cuánto tiempo toma implementar una solución de IA?',
    respuesta: 'El tiempo de implementación varía según la complejidad del proyecto, pero típicamente oscila entre 8 y 12 semanas desde el análisis hasta la implementación inicial.'
  },
  {
    pregunta: '¿Qué tipo de datos necesito para comenzar?',
    respuesta: 'Depende del tipo de solución, pero generalmente trabajamos con los datos que ya tienes. Nuestro equipo te guiará en la preparación y limpieza de datos necesaria.'
  },
  {
    pregunta: '¿Ofrecen capacitación para mi equipo?',
    respuesta: 'Sí, incluimos capacitación completa para tu equipo como parte de nuestra metodología, asegurando que puedan aprovechar al máximo la solución implementada.'
  },
  {
    pregunta: '¿Cómo miden el éxito del proyecto?',
    respuesta: 'Definimos KPIs claros desde la fase de análisis y monitoreamos continuamente estos indicadores para garantizar que la solución cumpla con los objetivos establecidos.'
  }
];
---

<MainLayout title="Metodología IA | GoRank">
  <!-- Breadcrumbs -->
  <div class="py-3 px-4 w-full max-w-7xl mx-auto">
    <ol class="flex flex-wrap items-center space-x-1 text-sm">
      <li class="flex items-center">
        <a href="/" class="text-gray-400 hover:text-white transition-colors">Inicio</a>
        <svg class="w-4 h-4 mx-2 text-gray-500" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path>
        </svg>
      </li>
      <li class="text-accent font-medium">Metodología IA</li>
    </ol>
  </div>

  <!-- Hero Section -->
  <section class="relative overflow-hidden min-h-[90vh] flex items-center bg-gradient-to-br from-background via-surface/70 to-background">
    <div class="absolute inset-0 -z-10 overflow-hidden">
      <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-5"></div>
      <!-- Efectos de partículas -->
      <div class="particle absolute w-4 h-4 rounded-full bg-accent/30 top-1/4 left-1/4 animate-float-random"></div>
      <div class="particle absolute w-3 h-3 rounded-full bg-primary/40 top-3/4 right-1/3 animate-float-random-delay"></div>
    </div>

    <div class="container mx-auto px-4 pt-24 pb-32 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Texto del Hero -->
        <div class="reveal-animation-left">
          <div class="inline-flex items-center gap-1 mb-6 px-3 py-1 rounded-full bg-accent/10 border border-accent/20 text-accent text-sm font-medium">
            <span class="relative flex h-2.5 w-2.5">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-accent"></span>
            </span>
            <span class="ml-1">Metodología Comprobada</span>
          </div>
          
          <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6 leading-tight">
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-white via-white to-accent/80">Nuestra Metodología</span>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent via-accent/90 to-primary block">para Proyectos de IA</span>
          </h1>
          
          <p class="text-lg text-gray-300 mb-8 leading-relaxed">
            En GoRank, hemos desarrollado una metodología probada que garantiza el éxito en la implementación de soluciones de IA. 
            Nuestro enfoque iterativo y centrado en resultados te permite comenzar a ver valor desde las primeras fases del proyecto.
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4">
            <a 
              href="#contacto" 
              class="btn-primary group inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
            >
              <span class="mr-2">Solicitar Asesoría</span>
              <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
              </svg>
            </a>
            
            <a 
              href="#metodologia" 
              class="btn-secondary inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
            >
              Conoce Nuestro Proceso
            </a>
          </div>
        </div>
        
        <!-- Imagen del Hero -->
        <div class="relative reveal-animation-right">
          <div class="relative z-10 rounded-2xl overflow-hidden shadow-2xl transform rotate-1 hover:rotate-0 transition-transform duration-500">
            <img 
              src="/img/metodologia-hero.jpg" 
              alt="Metodología de IA" 
              class="w-full h-auto"
              loading="eager"
              width="600"
              height="400"
            />
            <!-- Efecto de brillo en hover -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 hover:opacity-100 transition-opacity duration-500 flex items-end p-6">
              <div>
                <h3 class="text-white text-xl font-bold mb-2">Proceso de Implementación</h3>
                <p class="text-gray-300 text-sm">Desde el análisis hasta la optimización continua</p>
              </div>
            </div>
          </div>
          
          <!-- Elementos decorativos -->
          <div class="absolute -bottom-6 -left-6 w-32 h-32 bg-accent/10 rounded-full -z-10"></div>
          <div class="absolute -top-6 -right-6 w-24 h-24 bg-primary/10 rounded-full -z-10"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección de Metodología -->
  <section id="metodologia" class="py-20 bg-surface/50 relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-5"></div>
      <div class="absolute inset-0 bg-gradient-to-b from-background/0 via-background/80 to-background"></div>
    </div>
    
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Nuestra <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-primary">Metodología</span>
        </h2>
        <p class="text-lg text-gray-400 max-w-3xl mx-auto">
          Un enfoque probado para implementar con éxito soluciones de IA en tu negocio.
        </p>
      </div>
      
      <!-- Línea de tiempo de la metodología -->
      <div class="relative max-w-4xl mx-auto">
        <!-- Línea vertical -->
        <div class="absolute left-1/2 w-0.5 h-full bg-gradient-to-b from-accent/20 via-accent/40 to-accent/20 -translate-x-1/2"></div>
        
        <!-- Items de la línea de tiempo -->
        <div class="space-y-20">
          {fasesMetodologia.map((fase, index) => (
            <div class={`relative flex ${index % 2 === 0 ? 'flex-row' : 'flex-row-reverse'} items-center`}>
              <!-- Contenido -->
              <div class={`w-full md:w-5/12 p-6 rounded-2xl bg-surface/50 backdrop-blur-sm border border-white/5 transition-all duration-500 hover:shadow-[0_10px_30px_-10px_rgba(0,240,255,0.1)] hover:-translate-y-1 ${index % 2 === 0 ? 'md:mr-auto' : 'md:ml-auto'}`}>
                <div class="flex items-start">
                  <div class={`flex-shrink-0 w-12 h-12 rounded-xl bg-gradient-to-br ${fase.color} flex items-center justify-center text-2xl text-white mr-4`}>
                    {fase.icono}
                  </div>
                  <div>
                    <div class="flex items-center mb-2">
                      <h3 class="text-xl font-bold text-white">{fase.titulo}</h3>
                      <span class="ml-2 px-2 py-0.5 text-xs font-medium rounded-full bg-accent/10 text-accent">
                        {fase.duracion}
                      </span>
                    </div>
                    <p class="text-gray-400 mb-3">{fase.descripcion}</p>
                    <ul class="space-y-1.5">
                      {fase.pasos.map((paso) => (
                        <li class="flex items-center text-sm text-gray-400">
                          <svg class="w-3.5 h-3.5 text-accent mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
                          </svg>
                          {paso}
                        </li>
                      ))}
                    </ul>
                  </div>
                </div>
              </div>
              
              <!-- Punto en la línea -->
              <div class="hidden md:flex absolute left-1/2 -translate-x-1/2 w-6 h-6 rounded-full bg-gradient-to-br from-accent to-primary items-center justify-center text-white font-bold text-sm z-10">
                {index + 1}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Sección de Beneficios -->
  <section class="py-20 bg-gradient-to-b from-surface/50 to-background relative overflow-hidden">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Beneficios de Nuestra <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-primary">Metodología</span>
        </h2>
        <p class="text-lg text-gray-400 max-w-3xl mx-auto">
          Cada fase está diseñada para maximizar el valor y minimizar los riesgos en tu proyecto de IA.
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        {beneficios.map((beneficio, index) => (
          <div class="bg-surface/50 backdrop-blur-sm border border-white/5 rounded-2xl p-8 text-center transition-all duration-500 hover:shadow-[0_10px_30px_-10px_rgba(0,240,255,0.1)] hover:-translate-y-2">
            <div class="w-16 h-16 mx-auto mb-6 rounded-2xl bg-accent/10 flex items-center justify-center text-3xl text-accent">
              {beneficio.icono}
            </div>
            <h3 class="text-xl font-bold text-white mb-3">{beneficio.titulo}</h3>
            <p class="text-gray-400">{beneficio.descripcion}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Sección de Preguntas Frecuentes -->
  <section class="py-20 bg-surface/30 relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-5"></div>
    </div>
    
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Preguntas <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-primary">Frecuentes</span>
        </h2>
        <p class="text-lg text-gray-400 max-w-3xl mx-auto">
          Resolvemos tus dudas más comunes sobre nuestra metodología de implementación de IA.
        </p>
      </div>
      
      <div class="max-w-3xl mx-auto space-y-4">
        {preguntasFrecuentes.map((faq, index) => (
          <div class="group bg-surface/50 backdrop-blur-sm border border-white/5 rounded-xl overflow-hidden transition-all duration-300 hover:border-accent/30">
            <button 
              class="faq-toggle w-full flex justify-between items-center p-6 text-left focus:outline-none"
              aria-expanded="false"
              aria-controls={`faq-${index}`}
            >
              <span class="text-lg font-medium text-white">{faq.pregunta}</span>
              <svg class="w-5 h-5 text-accent transform transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            <div id={`faq-${index}`} class="faq-content hidden px-6 pb-6 -mt-2 text-gray-400">
              {faq.respuesta}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Sección de CTA -->
  <section id="contacto" class="py-20 bg-gradient-to-r from-primary/10 to-accent/10 relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-5"></div>
      <div class="absolute inset-0 bg-gradient-to-r from-primary/5 via-transparent to-accent/5"></div>
    </div>
    
    <div class="container mx-auto px-4 text-center">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
          ¿Listo para transformar tu negocio con IA?
        </h2>
        <p class="text-lg text-gray-300 mb-8">
          Nuestro equipo de expertos está listo para guiarte en cada paso del proceso de implementación de IA en tu organización.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/contacto" 
            class="btn-primary inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
          >
            <span class="mr-2">Solicitar Asesoría</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
            </svg>
          </a>
          
          <a 
            href="tel:+1234567890" 
            class="btn-secondary inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
          >
            <span class="mr-2">Hablar con un experto</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  /* Animaciones */
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  
  @keyframes float-delay {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
  }
  
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  
  .animate-float-delay {
    animation: float-delay 7s ease-in-out infinite;
    animation-delay: 1s;
  }
  
  .animate-float-random {
    animation: float 8s ease-in-out infinite;
    animation-delay: 0.5s;
  }
  
  .animate-float-random-delay {
    animation: float 9s ease-in-out infinite;
    animation-delay: 1.5s;
  }
  
  /* Efectos de hover */
  .btn-primary {
    @apply bg-gradient-to-r from-accent to-primary text-white rounded-xl font-medium transition-all duration-300 hover:shadow-[0_0_20px_rgba(0,240,255,0.5)] hover:-translate-y-1;
  }
  
  .btn-secondary {
    @apply bg-white/5 border border-white/10 text-white rounded-xl font-medium transition-all duration-300 hover:bg-white/10 hover:border-white/20 hover:-translate-y-1;
  }
  
  /* Efectos de revelación */
  .reveal-animation-left {
    @apply opacity-0 transform -translate-x-8 transition-all duration-1000 ease-out;
  }
  
  .reveal-animation-right {
    @apply opacity-0 transform translate-x-8 transition-all duration-1000 ease-out;
  }
  
  .reveal-animation-left.active, .reveal-animation-right.active {
    @apply opacity-100 translate-x-0;
  }
  
  /* Efectos de partículas */
  .particle {
    @apply rounded-full opacity-70;
  }
  
  /* Estilos para el acordeón de preguntas frecuentes */
  .faq-content {
    max-height: 0;
    overflow: hidden;
    transition: all 0.3s ease;
  }
  
  .faq-toggle[aria-expanded="true"] + .faq-content {
    max-height: 500px;
  }
  
  .faq-toggle[aria-expanded="true"] svg {
    transform: rotate(180deg);
  }
  
  /* Ajustes para la línea de tiempo */
  @media (max-width: 767px) {
    .timeline-item-content {
      width: 100%;
      margin-bottom: 1.5rem;
    }
    
    .timeline-item-dot {
      left: 0;
      transform: translateX(-50%);
    }
  }
</style>

<script>
  // Animación de revelación al hacer scroll
  document.addEventListener('DOMContentLoaded', () => {
    // Animación de elementos al hacer scroll
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('active');
        }
      });
    }, {
      threshold: 0.1
    });
    
    // Observar elementos con animación
    document.querySelectorAll('.reveal-animation-left, .reveal-animation-right').forEach(el => {
      observer.observe(el);
    });
    
    // Forzar la animación después de un pequeño retraso para elementos visibles al cargar
    setTimeout(() => {
      const elements = document.querySelectorAll('.reveal-animation-left, .reveal-animation-right');
      elements.forEach(el => {
        const rect = el.getBoundingClientRect();
        const isVisible = (
          rect.top >= 0 &&
          rect.left >= 0 &&
          rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
          rect.right <= (window.innerWidth || document.documentElement.clientWidth)
        );
        
        if (isVisible) {
          el.classList.add('active');
        }
      });
    }, 300);
    
    // Funcionalidad del acordeón de preguntas frecuentes
    document.querySelectorAll('.faq-toggle').forEach(button => {
      button.addEventListener('click', () => {
        const expanded = button.getAttribute('aria-expanded') === 'true' || false;
        button.setAttribute('aria-expanded', !expanded);
        const content = button.nextElementSibling;
        
        if (!expanded) {
          content.style.maxHeight = content.scrollHeight + 'px';
          content.classList.remove('hidden');
        } else {
          content.style.maxHeight = '0';
          // Esperar a que termine la animación para ocultar
          setTimeout(() => {
            if (button.getAttribute('aria-expanded') === 'false') {
              content.classList.add('hidden');
            }
          }, 300);
        }
      });
    });
  });
</script>

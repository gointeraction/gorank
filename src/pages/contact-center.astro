---
/*
  Archivo: contact-center.astro
  Propósito: Página de Soluciones de IA para Contact Center
  Estructura: Mantiene el estilo visual del sitio con elementos específicos para soluciones de contact center
*/
import MainLayout from '../layouts/MainLayout.astro';
import { Icon } from 'astro-icon/components';

// Datos de características de la solución
const caracteristicas = [
  {
    titulo: 'Asistentes Virtuales Inteligentes',
    descripcion: 'Agentes virtuales 24/7 que resuelven consultas frecuentes en tiempo real.',
    icono: '🤖',
    beneficios: [
      'Reducción de costos operativos',
      'Respuestas inmediatas',
      'Disponibilidad 24/7',
      'Escalabilidad automática'
    ]
  },
  {
    titulo: 'Análisis de Sentimientos',
    descripcion: 'Detecta el estado emocional de los clientes en tiempo real.',
    icono: '😊',
    beneficios: [
      'Mejora la satisfacción',
      'Identifica clientes insatisfechos',
      'Mide la calidad del servicio',
      'Genera alertas tempranas'
    ]
  },
  {
    titulo: 'Automatización de Procesos',
    descripcion: 'Automatiza tareas repetitivas y flujos de trabajo.',
    icono: '⚡',
    beneficios: [
      'Mayor eficiencia operativa',
      'Reducción de errores',
      'Optimización de recursos',
      'Tiempos de respuesta más rápidos'
    ]
  },
  {
    titulo: 'Análisis Predictivo',
    descripcion: 'Anticipa necesidades y comportamientos de los clientes.',
    icono: '🔮',
    beneficios: [
      'Mejora la retención',
      'Aumenta las ventas cruzadas',
      'Optimiza recursos',
      'Personaliza la experiencia'
    ]
  }
];

// Datos de beneficios
const beneficios = [
  {
    titulo: 'Soporte 24/7',
    descripcion: 'Atiende a tus clientes en cualquier momento del día.',
    icono: '🕒'
  },
  {
    titulo: 'Reducción de Costos',
    descripcion: 'Hasta un 30% de ahorro en operaciones de atención al cliente.',
    icono: '💰'
  },
  {
    titulo: 'Satisfacción del Cliente',
    descripcion: 'Mejora los índices de satisfacción con respuestas rápidas y precisas.',
    icono: '⭐'
  }
];
---

<MainLayout title="Soluciones IA para Contact Center | GoRank">
  <!-- Breadcrumbs para mejorar navegación -->
  <div class="py-3 px-4 w-full max-w-7xl mx-auto">
    <ol class="flex flex-wrap items-center space-x-1 text-sm">
      <li class="flex items-center">
        <a href="/" class="text-gray-400 hover:text-white transition-colors">Inicio</a>
        <svg class="w-4 h-4 mx-2 text-gray-500" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path>
        </svg>
      </li>
      <li class="text-accent font-medium">Soluciones para Contact Center</li>
    </ol>
  </div>

  <!-- Hero Section -->
  <section class="relative overflow-hidden min-h-[90vh] flex items-center">
    <!-- Fondo con efectos -->
    <div class="absolute inset-0 -z-10 overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-br from-background via-surface/70 to-background">
        <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-10"></div>
      </div>
      <!-- Efectos de partículas -->
      <div class="particle absolute w-4 h-4 rounded-full bg-accent/30 top-1/4 left-1/4 animate-float-random"></div>
      <div class="particle absolute w-3 h-3 rounded-full bg-primary/40 top-3/4 right-1/3 animate-float-random-delay"></div>
    </div>

    <div class="container mx-auto px-4 pt-24 pb-32 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Texto del Hero -->
        <div class="reveal-animation-left">
          <div class="inline-flex items-center gap-1 mb-6 px-3 py-1 rounded-full bg-accent/10 border border-accent/20 text-accent text-sm font-medium">
            <span class="relative flex h-2.5 w-2.5">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-accent"></span>
            </span>
            <span class="ml-1">Tecnología de Vanguardia</span>
          </div>
          
          <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6 leading-tight">
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-white via-white to-accent/80">Transforma tu</span>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent via-accent/90 to-primary block">Contact Center con IA</span>
          </h1>
          
          <p class="text-lg text-gray-300 mb-8 leading-relaxed">
            Soluciones de Inteligencia Artificial diseñadas para optimizar la atención al cliente, 
            mejorar la eficiencia operativa y ofrecer experiencias excepcionales a tus clientes.
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4">
            <a 
              href="#contacto" 
              class="btn-primary group inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
            >
              <span class="mr-2">Solicitar Demostración</span>
              <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
              </svg>
            </a>
            
            <a 
              href="#caracteristicas" 
              class="btn-secondary inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
            >
              Conoce más
            </a>
          </div>
          
          <!-- Estadísticas -->
          <div class="grid grid-cols-3 gap-4 mt-12">
            <div class="text-center">
              <div class="text-3xl font-bold text-accent mb-1">+30%</div>
              <div class="text-sm text-gray-400">Eficiencia</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-accent mb-1">24/7</div>
              <div class="text-sm text-gray-400">Disponibilidad</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-accent mb-1">+95%</div>
              <div class="text-sm text-gray-400">Satisfacción</div>
            </div>
          </div>
        </div>
        
        <!-- Imagen del Hero -->
        <div class="relative reveal-animation-right">
          <div class="relative z-10 rounded-2xl overflow-hidden shadow-2xl transform rotate-1 hover:rotate-0 transition-transform duration-500">
            <img 
              src="/img/contact-center-hero.jpg" 
              alt="Contact Center con IA" 
              class="w-full h-auto"
              loading="eager"
              width="600"
              height="400"
            />
            <!-- Efecto de brillo en hover -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 hover:opacity-100 transition-opacity duration-500 flex items-end p-6">
              <div>
                <h3 class="text-white text-xl font-bold mb-2">Dashboard de Control</h3>
                <p class="text-gray-300 text-sm">Monitorea el rendimiento en tiempo real</p>
              </div>
            </div>
          </div>
          
          <!-- Elementos decorativos -->
          <div class="absolute -bottom-6 -left-6 w-32 h-32 bg-accent/10 rounded-full -z-10"></div>
          <div class="absolute -top-6 -right-6 w-24 h-24 bg-primary/10 rounded-full -z-10"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección de Características -->
  <section id="caracteristicas" class="py-20 bg-surface/50 relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-5"></div>
      <div class="absolute inset-0 bg-gradient-to-b from-background/0 via-background/80 to-background"></div>
    </div>
    
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Soluciones <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-primary">Completas</span>
        </h2>
        <p class="text-lg text-gray-400 max-w-3xl mx-auto">
          Descubre cómo nuestra plataforma de IA puede transformar tu centro de contacto en un motor de crecimiento y satisfacción del cliente.
        </p>
      </div>
      
      <!-- Grid de características -->
      <div class="grid md:grid-cols-2 gap-8">
        {caracteristicas.map((caracteristica) => (
          <div class="group bg-surface/50 backdrop-blur-sm border border-white/5 rounded-2xl p-8 transition-all duration-500 hover:shadow-[0_10px_30px_-10px_rgba(0,240,255,0.2)] hover:-translate-y-2">
            <div class="flex items-start">
              <div class="flex-shrink-0 w-14 h-14 rounded-xl bg-accent/10 flex items-center justify-center text-2xl text-accent mr-6 transition-all duration-300 group-hover:bg-accent group-hover:text-white">
                {caracteristica.icono}
              </div>
              <div>
                <h3 class="text-xl font-bold text-white mb-2">{caracteristica.titulo}</h3>
                <p class="text-gray-400 mb-4">{caracteristica.descripcion}</p>
                <ul class="space-y-2">
                  {caracteristica.beneficios.map((beneficio) => (
                    <li class="flex items-center text-sm text-gray-400">
                      <svg class="w-4 h-4 text-accent mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                      </svg>
                      {beneficio}
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Sección de Beneficios -->
  <section class="py-20 bg-gradient-to-b from-surface/50 to-background relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-[url('/img/dot-pattern.svg')] opacity-5"></div>
    </div>
    
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Beneficios <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-primary">Clave</span>
        </h2>
        <p class="text-lg text-gray-400 max-w-3xl mx-auto">
          Nuestra solución de IA para contact centers está diseñada para ofrecer resultados tangibles y medibles.
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {beneficios.map((beneficio) => (
          <div class="bg-surface/50 backdrop-blur-sm border border-white/5 rounded-2xl p-8 text-center group hover:shadow-[0_10px_30px_-10px_rgba(0,240,255,0.1)] transition-all duration-500 hover:-translate-y-2">
            <div class="w-20 h-20 mx-auto mb-6 rounded-2xl bg-accent/10 flex items-center justify-center text-3xl text-accent group-hover:bg-accent group-hover:text-white transition-all duration-300">
              {beneficio.icono}
            </div>
            <h3 class="text-xl font-bold text-white mb-3">{beneficio.titulo}</h3>
            <p class="text-gray-400">{beneficio.descripcion}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Sección de CTA -->
  <section id="contacto" class="py-20 bg-gradient-to-r from-primary/10 to-accent/10 relative overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <div class="absolute inset-0 bg-[url('/img/grid-pattern.svg')] opacity-5"></div>
      <div class="absolute inset-0 bg-gradient-to-r from-primary/5 via-transparent to-accent/5"></div>
    </div>
    
    <div class="container mx-auto px-4 text-center">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
          ¿Listo para transformar tu Contact Center con IA?
        </h2>
        <p class="text-lg text-gray-300 mb-8">
          Agenda una demostración personalizada y descubre cómo podemos ayudarte a alcanzar tus objetivos de negocio.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/contacto" 
            class="btn-primary inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
          >
            <span class="mr-2">Solicitar Demostración</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
            </svg>
          </a>
          
          <a 
            href="tel:+1234567890" 
            class="btn-secondary inline-flex items-center justify-center px-8 py-4 text-lg font-medium"
          >
            <span class="mr-2">Llámanos</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  /* Animaciones */
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  
  @keyframes float-delay {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
  }
  
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  
  .animate-float-delay {
    animation: float-delay 7s ease-in-out infinite;
    animation-delay: 1s;
  }
  
  .animate-float-random {
    animation: float 8s ease-in-out infinite;
    animation-delay: 0.5s;
  }
  
  .animate-float-random-delay {
    animation: float 9s ease-in-out infinite;
    animation-delay: 1.5s;
  }
  
  /* Efectos de hover */
  .btn-primary {
    @apply bg-gradient-to-r from-accent to-primary text-white rounded-xl font-medium transition-all duration-300 hover:shadow-[0_0_20px_rgba(0,240,255,0.5)] hover:-translate-y-1;
  }
  
  .btn-secondary {
    @apply bg-white/5 border border-white/10 text-white rounded-xl font-medium transition-all duration-300 hover:bg-white/10 hover:border-white/20 hover:-translate-y-1;
  }
  
  /* Efectos de revelación */
  .reveal-animation-left {
    @apply opacity-0 transform -translate-x-8 transition-all duration-1000 ease-out;
  }
  
  .reveal-animation-right {
    @apply opacity-0 transform translate-x-8 transition-all duration-1000 ease-out;
  }
  
  .reveal-animation-left.active, .reveal-animation-right.active {
    @apply opacity-100 translate-x-0;
  }
  
  /* Efectos de partículas */
  .particle {
    @apply rounded-full opacity-70;
  }
</style>

<script>
  // Animación de revelación al hacer scroll
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('active');
        }
      });
    }, {
      threshold: 0.1
    });
    
    // Observar elementos con animación
    document.querySelectorAll('.reveal-animation-left, .reveal-animation-right').forEach(el => {
      observer.observe(el);
    });
    
    // Forzar la animación después de un pequeño retraso para elementos visibles al cargar
    setTimeout(() => {
      const elements = document.querySelectorAll('.reveal-animation-left, .reveal-animation-right');
      elements.forEach(el => {
        const rect = el.getBoundingClientRect();
        const isVisible = (
          rect.top >= 0 &&
          rect.left >= 0 &&
          rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
          rect.right <= (window.innerWidth || document.documentElement.clientWidth)
        );
        
        if (isVisible) {
          el.classList.add('active');
        }
      });
    }, 300);
  });
</script>
